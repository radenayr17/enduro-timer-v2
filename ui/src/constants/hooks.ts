export enum RaceApiHooks {
  getRaces = "getRaces",
  getRace = "getRace",
  getRacers = "getRacers",
}

export enum StageApiHooks {
  getStages = "getStages",
  getStage = "getStage",
  getStageRecords = "getStageRecords",
}
